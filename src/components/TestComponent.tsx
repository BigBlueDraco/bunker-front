import React from "react";
import styled from "styled-components";

const AppWrap = styled.div`
  width: 150px;
  margin: 0 auto;
`;

function TestComponent() {
  return <AppWrap>Bunker Menu Front</AppWrap>;
}

export default TestComponent;
